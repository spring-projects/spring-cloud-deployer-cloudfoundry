logging:
  level:
    cloudfoundry-client: DEBUG
spring:
  cloud:
    deployer:
      cloudfoundry:
        domain: api.sys.deer.wild.cf-app.com
        organization: system
        space: dev
        apiEndpoint: https://api.sys.deer.wild.cf-app.com
        username: michael
        password: passmichael
        skipSslValidation: true
        services: my_mysql
        defaults:
          services: my_mysql

