logging:
  level:
    cloudfoundry-client: DEBUG
spring:
  cloud:
    deployer:
      cloudfoundry:
        domain: api.sys.crane.wild.cf-app.com
        organization: system
        space: system
        apiEndpoint: https://api.sys.crane.wild.cf-app.com
        username: mminella
        password: passmminella
        skipSslValidation: true
        services: my_mysql
        defaults:
          services: my_mysql

